[package]
name = "spel-framework"
version = "0.6.0"
edition = "2021"
description = "Developer framework for building SPEL programs (like Anchor for Solana)"

[features]
default = []
host = ["dep:nssa", "spel-framework-core/host"]

[dependencies]
spel-framework-core = { path = "../spel-framework-core" }
spel-framework-macros = { path = "../spel-framework-macros" }
nssa_core = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", features = ["host"], package = "lee_core" }
nssa = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", optional = true, package = "lee" }
borsh = { version = "1.0", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
spel-client-gen = { path = "../spel-client-gen" }
# The e2e suite drives the CLI's IDL entry point directly, which lives
# behind `idl-gen`. Declared here so the tests do not depend on feature
# unification with the proc-macro crate to switch it on.
spel-framework-core = { path = "../spel-framework-core", features = ["idl-gen"] }

[lints]
workspace = true
